import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pendaftaran_mahasiswa',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
